rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All data access uses authorized server endpoints. No browser database access.
    match /{document=**} { allow read, write: if false; }
  }
}
